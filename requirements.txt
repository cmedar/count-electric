# Ingestion
requests
boto3==1.35.99

# Orchestration
# Airflow must be installed separately with constraints:
# pip install "apache-airflow==2.10.5" --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.10.5/constraints-3.12.txt"

# Databricks
databricks-sdk
databricks-sql-connector

# Dashboard
streamlit>=1.37.0
plotly
pandas
pyarrow
# s3fs/aiobotocore/botocore must be pinned together — unpinned causes pip to
# backtrack through 50+ versions downloading source tarballs, exhausting disk
aiobotocore==2.15.2
s3fs==2024.10.0

# Utilities
python-dotenv
